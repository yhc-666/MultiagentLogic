rule1
	foreach
		facts.Young($x, True)
	assert
		facts.Furry($x, True)

rule2
	foreach
		facts.Nice($x, True)
		facts.Furry($x, True)
	assert
		facts.Green($x, True)

rule3
	foreach
		facts.Green($x, True)
	assert
		facts.Nice($x, True)

rule4
	foreach
		facts.Nice($x, True)
		facts.Green($x, True)
	assert
		facts.Big($x, True)

rule5
	foreach
		facts.Green($x, True)
	assert
		facts.Smart($x, True)

rule6
	foreach
		facts.Big($x, True)
		facts.Young($x, True)
	assert
		facts.Round($x, True)

rule7
	foreach
		facts.Green($x, True)
	assert
		facts.Big($x, True)

rule8
	foreach
		facts.Young(Harry, True)
	assert
		facts.Furry(Harry, True)

rule9
	foreach
		facts.Furry($x, True)
		facts.Smart($x, True)
	assert
		facts.Nice($x, True)